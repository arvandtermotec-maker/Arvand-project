# Arvand Termo Tec

## Run locally

```bash
npm install
npm run dev
```

## Before production

1. Copy `.env.example` to `.env.local`
2. Fill SMTP values
3. Put final assets into `public/` if you want to replace placeholders
